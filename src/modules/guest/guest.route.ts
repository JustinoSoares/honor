// import express from 'express';
// import { GuestController } from './guest.controller';
// import * as schema from './guest.schema';
// import { validate } from '../../middleware/validate';

// const guestController = new GuestController();
// const guestRouter = express.Router();

// guestRouter.post('/create/:event_id', validate(schema.GuestSchema), guestController.createGuest);
// guestRouter.get('/list/:event_id', guestController.getGuestsByEventId);
// guestRouter.get('/each/:guest_id', guestController.getGuestById);
// guestRouter.delete('/delete/:guest_id', guestController.deleteGuest);
// export default guestRouter;

